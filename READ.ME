TicTacToe played via Command Line Interface!

User instructions:

Invoke creation of board in terminal by running makeBoard.
Calling 'board' will show an empty board object of 3 arrays, each array with starting value of '-'
Use functions xTurn and oTurn with x and y coordinates as parameters
    - Example: xTurn(0, 0) || yTurn(2, 2)
Winner will be automatically announced once three of the same pieces have been connected